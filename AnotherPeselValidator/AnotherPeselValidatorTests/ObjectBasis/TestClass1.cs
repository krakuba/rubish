using Microsoft.VisualStudio.TestTools.UnitTesting;
using AnotherPeselValidator.ObjectBasis;
using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.Linq;
using System.Security.Cryptography.X509Certificates;
using System.Text;
using System.Threading.Tasks;

namespace AnotherPeselValidator.ObjectBasis.Tests
{
    [TestClass()]
    public class PeselTests
    {
        [TestMethod()]
        public void PeselTestOne()
        {
            //IList<Pesel> peselList = new List<Pesel>();

            /*
            PeselCheck check = new PeselCheck();
            Pesel p1Pesel = new Pesel("92101004612");
            Pesel p2Pesel = new Pesel("12312321333");
            Pesel p3Pesel = new Pesel("434343aas33");

            check.CheckPesel(p1Pesel);
            Debug.WriteLine(check.CheckPesel(p1Pesel));
            check.CheckPesel(p2Pesel);
            Debug.WriteLine(check.CheckPesel(p2Pesel));
            check.CheckPesel(p3Pesel);
            Debug.WriteLine(check.CheckPesel(p3Pesel));
            */
           }
    }
}